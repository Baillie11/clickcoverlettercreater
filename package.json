{
  "name": "click-cover-letter-creator",
  "version": "1.0.0",
  "private": true,
  "description": "Cover letter builder with themes, preview, and local DB persistence.",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "dev:browsersync": "concurrently -k -n server,browser -c magenta,cyan \"nodemon server.js\" \"browser-sync start --proxy http://localhost:5050 --files index.html,profile.html,styles.css,script.js --port 3000 --no-notify\""
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^12.4.1",
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "openai": "^6.7.0"
  },
  "devDependencies": {
    "browser-sync": "^3.0.4",
    "concurrently": "^9.2.1",
    "nodemon": "^3.1.10"
  }
}
