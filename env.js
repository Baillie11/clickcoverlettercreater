/* Frontend runtime config */
/* 
 * For VentraIP: If backend is NOT running, comment out the line below
 * to use the Heroku fallback in script.js and dashboard.js
 * 
 * For same-origin deployment: window.ENV_API_BASE = '';
 * For external API: window.ENV_API_BASE = 'https://your-api-domain.com';
 */

// COMMENT THIS OUT if backend is not running on VentraIP:
// window.ENV_API_BASE = '';
